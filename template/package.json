{
  "name": "{{basename}}",
  "description": "{{description}}",
  "repository": "git://github.com/micro-js/{{basename}}.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags{{#npm}} && npm publish{{/npm}}"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
